{
  "name": "filament-inventory",
  "private": true,
  "type": "module",
  "description": "Inventory markdown → Spoolman JSON → API (root CLI: node cli.js)",
  "bin": {
    "filament-inventory": "./cli.js"
  },
  "scripts": {
    "cli": "node cli.js",
    "inventory:migrate": "node cli.js migrate",
    "spoolman:push": "node cli.js push",
    "spoolman:reload": "node cli.js reload",
    "spoolman:cleanup": "node cli.js cleanup"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
